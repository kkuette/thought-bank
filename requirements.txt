# Dependencies for the active project: deepseek_v4_mini (dual-stream thought memory).
# Install:  pip install -r requirements.txt

# Deep learning
torch>=2.1.0

# Tokenizer + datasets (DeepSeek-V3 tokenizer, HF streaming)
transformers>=4.45.0
tokenizers>=0.15.0
datasets>=2.16.0
sentencepiece>=0.1.99        # safety net for some HF tokenizers

# Training utilities
tensorboard>=2.15.0          # torch.utils.tensorboard backend (imported by train.py)
numpy>=1.24.0
pyyaml>=6.0
tqdm>=4.66.0

# ── Legacy diffusion prototype (diffusion_thought_tensor/) — not maintained ──
# The earlier prototype additionally needed: diffusers, pytorch-lightning,
# x-transformers, hydra-core, omegaconf, wandb, trl, scikit-learn, scipy,
# matplotlib, seaborn, plotly. Install those manually only if running that code.
